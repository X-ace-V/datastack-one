import { StepShell } from "../components/StepShell";

/** Step 6 — serve the final table via preview, CSV, and REST. Wired up in T5.4. */
export function ServePage() {
  return <StepShell slug="serve" />;
}
