import { StepShell } from "../components/StepShell";

/** Step 2 — connect and profile a CSV source. Wired up in T2.2–T2.4. */
export function ConnectPage() {
  return <StepShell slug="connect" />;
}
