import { StepShell } from "../components/StepShell";

/** Step 4 — review generated artifacts before execution. Wired up in T3.5. */
export function ReviewPage() {
  return <StepShell slug="review" />;
}
